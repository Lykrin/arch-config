# Wayland + toolkits
export MOZ_ENABLE_WAYLAND=1
export SDL_VIDEODRIVER=wayland
export CLUTTER_BACKEND=wayland
export GDK_BACKEND=wayland
export QT_QPA_PLATFORM=wayland
export QT_AUTO_SCREEN_SCALE_FACTOR=1
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1

# GTK force dark style and theme
export GTK_THEME=catppuccin-mocha-teal-standard+default
export GTK_APPLICATION_PREFER_DARK_THEME=1
export ADW_DEBUG_COLOR_SCHEME=prefer-dark
# Qt platform theme via qt6ct/qt5ct (will read Qt theme/colors set there)
export QT_QPA_PLATFORMTHEME=qt6ct
export XCURSOR_THEME=Bibata-Modern-Classic
export XCURSOR_SIZE=36
# Fallback to qt5ct for Qt5 apps (qt6ct handles Qt6)
#QT_QPA_PLATFORMTHEME_FALLBACK=qt5ct

# Prefer Wayland where possible
export GTK_USE_PORTAL=1

# Kvantum (optional; if you choose Kvantum in qt6ct)
#QT_STYLE_OVERRIDE=kvantum

# AMD VA-API for RDNA2/RDNA3
#export LIBVA_DRIVER_NAME=amdgpu
